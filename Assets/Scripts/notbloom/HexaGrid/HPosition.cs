using System;
namespace notbloom.HexagonalMap
{
    [Serializable]
    public class HPosition
    {
        // public float x;
        // public float y;

        // public override string ToString() => "HPosition[" + x.ToString() + "|" + y.ToString() + "]";

        // public HPosition(float _x, float _y)
        // {
        //     x = _x;
        //     y = _y;
        // }
        // public static float SquaredDistance(HPosition a, HPosition b) => (b.x - a.x) * (b.x - a.x) + (b.y - a.y) * (b.y - a.y);

        // //  public static float SquaredDistance(this HPosition a, HPosition b) => SquaredDistance(a, b);

    }
}